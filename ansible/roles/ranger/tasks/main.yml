- name: Install ranger
  become: true
  package:
    name:
      - ranger
      - trash-cli
    state: present

- name: Link ranger config
  file:
    src: "{{ path_dotfiles }}/ranger"
    dest: "{{ path_config_ranger }}"
    state: link
    force: yes
